extends default

block content
  div.reviewContainer
    h3#pageTitle 
        | #{Deck}
    p#question #{Question}
    p#answer #{Answer}
    form(method='post' action='/api/multireview/a')
        // name section of the buttons below is passed in body of html request 
        // can be accessed in .get and .post etc as req.body.NAME 
        button.btn.btn-review(type='submit') Next